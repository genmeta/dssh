[package]
name = "genmeta-ssh"
description = "SSH3 core session, channel, and wire-format foundation"
version = "0.1.0"
edition = "2024"

[dependencies]
h3x = { path = "../h3x", features = ["remoc", "serde"] }
http = "1"
http-body-util = "0.1"
nix = { version = "0.29", features = ["term", "ioctl", "signal", "fs", "process", "user"] }
snafu = "0.9"
tracing = "0.1"
bytes = { version = "1", features = ["serde"] }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros", "sync", "io-util", "process"] }
base64 = "0.22"
remoc = { version = "0.18", default-features = false, features = [
    "remoc_macro",
    "rfn",
    "rtc",
    "serde",
    "default-codec-json",
] }
pam-client2 = { version = "0.5", default-features = false, optional = true }
serde = { version = "1", features = ["derive"] }
futures = "0.3"

[features]
pam = ["dep:pam-client2"]

[dev-dependencies]
clap = { version = "4.6.0", features = ["derive"] }
serde_json = "1"
tempfile = "3"
tower-service = "0.3"
tracing-subscriber = "0.3"
